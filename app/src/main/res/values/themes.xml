<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme — full screen, edge-to-edge, dark background -->
    <style name="Theme.GoosebumpsRider" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Splash theme — same as base, shown before Compose takes over -->
    <style name="Theme.GoosebumpsRider.Splash" parent="Theme.GoosebumpsRider">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:windowFullscreen">false</item>
    </style>

</resources>
